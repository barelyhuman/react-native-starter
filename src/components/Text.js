import styled from 'styled-components/native';
import {color, fontFamily,fontSize, layout, space} from 'styled-system';

export const Text = styled.Text`
  ${space}
  ${color}
${layout}
${fontSize}
${fontFamily}
`;
